---
import Layout from '../../../../layouts/Layout.astro';
import { useTranslations, languages } from '../../../../i18n/ui';
import BackToStudioButton from '../../../../components/BackToStudioButton.astro';
import AppBackgroundBlurs from '../../../../components/AppBackgroundBlurs.astro';

// 원천 데이터 및 번역 사전 임포트
import { habits } from '../../../../data/habits';
import { buildsnapTranslations } from '../../../../i18n/buildsnapTranslations';
import habitsTranslations from '../../../../i18n/habitsTranslations.json';

export function getStaticPaths() {
  return Object.keys(languages).map((lang) => ({
    params: { lang },
  }));
}

const { lang } = Astro.params;
const currentLang = (lang as string) || 'ko';

const t = (key: string) => {
  return buildsnapTranslations[currentLang]?.[key] || buildsnapTranslations['ko']?.[key] || key;
};

const tUi = useTranslations(currentLang as any);

const getTranslatedMentorName = (id: string, defaultName: string) => {
  return (habitsTranslations as any)[id]?.[currentLang]?.name || defaultName;
};

// 1. 빌드 타임 통계 사전 분석 (Pre-aggregation)
const totalHabits = habits.length;

// A. timeOfDay 집계
const timeOfDayCounts = { morning: 0, afternoon: 0, evening: 0, night: 0, anytime: 0 };
habits.forEach(h => {
  const key = h.timeOfDay === 'evening' ? 'evening' :
              h.timeOfDay === 'night' ? 'night' :
              h.timeOfDay === 'afternoon' ? 'afternoon' :
              h.timeOfDay === 'morning' ? 'morning' : 'anytime';
  timeOfDayCounts[key]++;
});

// B. era 집계
const eraCounts = { ancient: 0, renaissance: 0, earlyModern: 0, modern: 0, contemporary: 0 };
habits.forEach(h => {
  const eraStr = (h.era || '').toLowerCase();
  if (eraStr.includes('고대')) eraCounts.ancient++;
  else if (eraStr.includes('르네상스')) eraCounts.renaissance++;
  else if (eraStr.includes('근세')) eraCounts.earlyModern++;
  else if (eraStr.includes('근대')) eraCounts.modern++;
  else eraCounts.contemporary++; // 현대 및 contemporary 통합
});

// C. 각 시대별 매칭 위인 명단 빌드타임 전처리
const eraMentors = {
  ancient: habits.filter(h => (h.era || '').toLowerCase().includes('고대')).map(h => getTranslatedMentorName(h.id, h.name)),
  renaissance: habits.filter(h => (h.era || '').toLowerCase().includes('르네상스')).map(h => getTranslatedMentorName(h.id, h.name)),
  earlyModern: habits.filter(h => (h.era || '').toLowerCase().includes('근세')).map(h => getTranslatedMentorName(h.id, h.name)),
  modern: habits.filter(h => (h.era || '').toLowerCase().includes('근대')).map(h => getTranslatedMentorName(h.id, h.name)),
  contemporary: habits.filter(h => !(h.era || '').toLowerCase().includes('고대') && !(h.era || '').toLowerCase().includes('르네상스') && !(h.era || '').toLowerCase().includes('근세') && !(h.era || '').toLowerCase().includes('근대')).map(h => getTranslatedMentorName(h.id, h.name))
};

// D. 준비 난이도 (requiredItems 개수 기준)
let easyCount = 0;
let hardCount = 0;
habits.forEach(h => {
  const itemsLen = h.requiredItems ? h.requiredItems.length : 0;
  if (itemsLen <= 1 || h.tags.includes('#쉬움')) {
    easyCount++;
  } else {
    hardCount++;
  }
});

const easyPercent = Math.round((easyCount / totalHabits) * 100);
const hardPercent = 100 - easyPercent; // 보정 완료

// E. 행동 카테고리 집계 (태그 키워드 매핑)
let focusCount = 0;      // 집중력 및 생산성
let creativeCount = 0;   // 창의성 및 아이디어
let simplicityCount = 0; // 단순화 및 의사결정
let mindCount = 0;       // 휴식 및 사색

habits.forEach(h => {
  const tagsStr = h.tags.join(' ');
  if (tagsStr.includes('집중') || tagsStr.includes('몰입') || tagsStr.includes('생산성')) focusCount++;
  if (tagsStr.includes('창의') || tagsStr.includes('아이디어') || tagsStr.includes('글쓰기') || tagsStr.includes('기록') || tagsStr.includes('메모')) creativeCount++;
  if (tagsStr.includes('단순') || tagsStr.includes('의사결정')) simplicityCount++;
  if (tagsStr.includes('휴식') || tagsStr.includes('사색') || tagsStr.includes('명상') || tagsStr.includes('수면') || tagsStr.includes('이완')) mindCount++;
});

const totalCategorySum = (focusCount + creativeCount + simplicityCount + mindCount) || 1;
const focusPercent = Math.round((focusCount / totalCategorySum) * 100);
const creativePercent = Math.round((creativeCount / totalCategorySum) * 100);
const simplicityPercent = Math.round((simplicityCount / totalCategorySum) * 100);
const mindPercent = 100 - (focusPercent + creativePercent + simplicityPercent); // 카테고리 총합 100% 보정 완료

// F. 시간대별 반올림 오차 보정 완료형 변수 선언
const morningPercent = Math.round((timeOfDayCounts.morning / totalHabits) * 100);
const afternoonPercent = Math.round((timeOfDayCounts.afternoon / totalHabits) * 100);
const eveningPercent = Math.round((timeOfDayCounts.evening / totalHabits) * 100);
const nightPercent = Math.round((timeOfDayCounts.night / totalHabits) * 100);
const anytimePercent = 100 - (morningPercent + afternoonPercent + eveningPercent + nightPercent); // 5대 시간대 총합 100% 보정 완료

// 클라이언트 사이드 스크립트 전달용 번역 상수
const runtimeTranslations = {
  routineSuffix: t('buildsnap.routine.suffix'),
  calendarSuffix: t('buildsnap.calendar.suffix'),
  step1: t('buildsnap.milestone.step1'),
  step2: t('buildsnap.milestone.step2'),
  step3: t('buildsnap.milestone.step3'),
  canvasStep1: t('buildsnap.milestone.canvas.step1'),
  canvasStep2: t('buildsnap.milestone.canvas.step2'),
  canvasStep3: t('buildsnap.milestone.canvas.step3'),
  canvasBranding: 'Designed by SelaLink Studio',
  timeMorning: t('buildsnap.time.morning'),
  timeAfternoon: t('buildsnap.time.afternoon'),
  timeEvening: t('buildsnap.time.evening'),
  timeNight: t('buildsnap.time.night'),
  timeAnytime: t('buildsnap.time.anytime'),
  statsBadge: t('buildsnap.matcher.stats.badge'),
  statsBadgeNone: t('buildsnap.matcher.stats.badge.none'),
  cycleGuide: t('buildsnap.matcher.cycle.guide'),
  toastReset: t('buildsnap.matcher.toast.reset')
};
---

<Layout 
  title={t('buildsnap.title')} 
  lang={currentLang}
  description={t('buildsnap.subtitle')}
  canonicalUrl={`https://selalink.net/${currentLang}/apps/buildsnap/`}
  ogType="software"
  image="/assets/images/og/buildself.png"
  appId="buildsnap"
>
  <!-- CSS Animations for premium charts -->
  <style is:global>
    @keyframes waveMove {
      0% { transform: translateX(0) translateZ(0) scaleY(1); }
      50% { transform: translateX(-25%) translateZ(0) scaleY(1.05); }
      100% { transform: translateX(-50%) translateZ(0) scaleY(1); }
    }
    .animate-wave {
      animation: waveMove 4s cubic-bezier(0.36, 0.45, 0.63, 0.53) infinite;
    }
    @keyframes rotateSpin {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }
    .animate-spin-glow {
      animation: rotateSpin 20s linear infinite;
    }
  </style>

  <main class="max-w-[1250px] mx-auto px-4 pt-24 pb-8 md:pt-28 md:pb-16 relative">
    <AppBackgroundBlurs appId="buildsnap" />

    <!-- Header / Go Back Button -->
    <div class="relative z-10">
      <BackToStudioButton lang={currentLang} class="mb-8" />
    </div>

    <!-- Title Header -->
    <div class="text-center mb-10 md:mb-16 relative z-10 select-none">
      <h1 class="text-3xl sm:text-5xl md:text-6xl font-black tracking-tight mb-3 bg-gradient-to-r from-indigo-600 to-indigo-400 dark:from-indigo-500 dark:to-indigo-300 bg-clip-text text-transparent">
        {t('buildsnap.title')}
      </h1>
      <p class="text-sm sm:text-base md:text-lg text-slate-650 dark:text-slate-200 max-w-3xl mx-auto leading-relaxed">
        {t('buildsnap.subtitle')}
      </p>
    </div>

    <!-- 1. Stats Dashboard Section (Premium UI with Accordion) -->
    <section class="app-card-neon p-6 md:p-8 mb-8 md:mb-12 relative z-10 border border-slate-200 dark:border-slate-800/80 rounded-3xl bg-slate-50/50 dark:bg-slate-900/10">
      
      <!-- Accordion Toggle Header Trigger -->
      <div id="btnToggleStats" class="flex items-center justify-between cursor-pointer select-none group">
        <div class="text-left">
          <h2 class="text-xl sm:text-2xl font-black text-slate-855 dark:text-slate-100 flex items-center gap-2">
            <span>{t('buildsnap.stats.title')}</span>
          </h2>
          <p class="text-xs sm:text-sm text-slate-700 dark:text-slate-400 mt-1 font-bold">
            {t('buildsnap.stats.desc').replace('$1', habits.length.toString())}
          </p>
        </div>
        <div class="p-2 rounded-xl bg-slate-250/60 dark:bg-slate-800/60 border border-slate-250/30 dark:border-slate-750/30 text-slate-500 group-hover:text-indigo-500 transition-all shrink-0 flex items-center justify-center">
          <span id="statsToggleArrow" class="sr-only">▼</span>
          <svg id="chevronStatsIcon" class="w-5 h-5 transition-transform duration-300 transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
          </svg>
        </div>
      </div>

      <!-- Accordion Content Container -->
      <div id="statsContainer" class="hidden mt-6 md:mt-8 transition-all duration-300">
        
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 md:gap-8">
          <!-- Chart A: timeOfDay Distribution (Radial Time Dial with High Contrast) -->
          <div class="bg-white/70 dark:bg-slate-900/40 backdrop-blur-md border border-slate-200 dark:border-slate-800/80 p-6 rounded-2xl flex flex-col justify-between select-none relative group transition-all duration-300 hover:border-indigo-500/40 shadow-sm">
            <h3 class="text-sm font-black text-slate-855 dark:text-slate-200 tracking-tight border-b border-slate-200 dark:border-slate-800 pb-2 mb-4">
              🕒 {t('buildsnap.stats.time')}
            </h3>
            <div class="flex flex-col items-center justify-center flex-1 py-3">
              <div class="relative w-40 h-40">
                <svg viewBox="0 0 200 200" class="w-full h-full transform -rotate-90">
                  <!-- Clock background circle -->
                  <circle cx="100" cy="100" r="80" class="stroke-slate-200 dark:stroke-slate-855 fill-none" stroke-width="12" />
                  
                  <!-- Anytime outline glow circle -->
                  <circle cx="100" cy="100" r="90" class="stroke-indigo-500/20 fill-none" stroke-width="2" stroke-dasharray="6 4" />

                  <!-- 1. Morning Arc -->
                  <circle cx="100" cy="100" r="80" 
                    class="stroke-violet-500 fill-none transition-all duration-350 cursor-pointer hover:stroke-violet-400" 
                    stroke-width="14" 
                    stroke-dasharray="502.6" 
                    stroke-dashoffset={502.6 * (1 - (morningPercent / 100))} 
                    transform="rotate(0, 100, 100)"
                    id="arcMorning"
                  />

                  <!-- 2. Afternoon Arc -->
                  <circle cx="100" cy="100" r="80" 
                    class="stroke-amber-500 fill-none transition-all duration-350 cursor-pointer hover:stroke-amber-400" 
                    stroke-width="14" 
                    stroke-dasharray="502.6" 
                    stroke-dashoffset={502.6 * (1 - (afternoonPercent / 100))} 
                    transform={`rotate(${360 * (morningPercent / 100)}, 100, 100)`}
                    id="arcAfternoon"
                  />

                  <!-- 3. Evening Arc -->
                  <circle cx="100" cy="100" r="80" 
                    class="stroke-pink-500 fill-none transition-all duration-350 cursor-pointer hover:stroke-pink-400" 
                    stroke-width="14" 
                    stroke-dasharray="502.6" 
                    stroke-dashoffset={502.6 * (1 - (eveningPercent / 100))} 
                    transform={`rotate(${360 * ((morningPercent + afternoonPercent) / 100)}, 100, 100)`}
                    id="arcEvening"
                  />

                  <!-- 4. Night Arc -->
                  <circle cx="100" cy="100" r="80" 
                    class="stroke-blue-500 fill-none transition-all duration-350 cursor-pointer hover:stroke-blue-400" 
                    stroke-width="14" 
                    stroke-dasharray="502.6" 
                    stroke-dashoffset={502.6 * (1 - (nightPercent / 100))} 
                    transform={`rotate(${360 * ((morningPercent + afternoonPercent + eveningPercent) / 100)}, 100, 100)`}
                    id="arcNight"
                  />

                  <!-- 5. Anytime Arc (Completes the 100% MECE circle) -->
                  <circle cx="100" cy="100" r="80" 
                    class="stroke-indigo-500 fill-none transition-all duration-350 cursor-pointer hover:stroke-indigo-400" 
                    stroke-width="14" 
                    stroke-dasharray="502.6" 
                    stroke-dashoffset={502.6 * (1 - (anytimePercent / 100))} 
                    transform={`rotate(${360 * ((morningPercent + afternoonPercent + eveningPercent + nightPercent) / 100)}, 100, 100)`}
                    opacity="0.45"
                    id="arcAnytime"
                  />
                </svg>
                <div class="absolute inset-0 flex flex-col items-center justify-center text-center pointer-events-none">
                  <span class="text-[11px] font-black text-slate-600 dark:text-slate-400 uppercase tracking-widest" id="radialLabel">Total</span>
                  <span class="text-xl font-black text-slate-855 dark:text-white mt-0.5" id="radialValue">{totalHabits}</span>
                </div>
              </div>
              
              <!-- timeOfDay color legend for high readability -->
              <div class="flex flex-wrap justify-center gap-x-3.5 gap-y-2 mt-5 text-[10px] sm:text-[11px] font-extrabold select-none border-t border-slate-200/60 dark:border-slate-800/60 pt-4 w-full">
                <div class="flex items-center gap-1.5 text-slate-600 dark:text-slate-400">
                  <span class="w-2.5 h-2.5 rounded-full bg-violet-500 shrink-0"></span>
                  <span>{t('buildsnap.time.morning')}</span>
                  <span class="font-black text-slate-855 dark:text-slate-200">{morningPercent}%</span>
                </div>
                <div class="flex items-center gap-1.5 text-slate-600 dark:text-slate-400">
                  <span class="w-2.5 h-2.5 rounded-full bg-amber-500 shrink-0"></span>
                  <span>{t('buildsnap.time.afternoon')}</span>
                  <span class="font-black text-slate-855 dark:text-slate-200">{afternoonPercent}%</span>
                </div>
                <div class="flex items-center gap-1.5 text-slate-600 dark:text-slate-400">
                  <span class="w-2.5 h-2.5 rounded-full bg-pink-500 shrink-0"></span>
                  <span>{t('buildsnap.time.evening')}</span>
                  <span class="font-black text-slate-855 dark:text-slate-200">{eveningPercent}%</span>
                </div>
                <div class="flex items-center gap-1.5 text-slate-600 dark:text-slate-400">
                  <span class="w-2.5 h-2.5 rounded-full bg-blue-500 shrink-0"></span>
                  <span>{t('buildsnap.time.night')}</span>
                  <span class="font-black text-slate-855 dark:text-slate-200">{nightPercent}%</span>
                </div>
                <div class="flex items-center gap-1.5 text-slate-600 dark:text-slate-400 opacity-60">
                  <span class="w-2.5 h-2.5 rounded-full bg-indigo-500/50 shrink-0"></span>
                  <span>{t('buildsnap.time.anytime')}</span>
                  <span class="font-black text-slate-855 dark:text-slate-200">{anytimePercent}%</span>
                </div>
              </div>
            </div>
          </div>

          <!-- Chart B: category Distribution -->
          <div class="bg-white/70 dark:bg-slate-900/40 backdrop-blur-md border border-slate-200 dark:border-slate-800/80 p-6 rounded-2xl flex flex-col justify-between select-none transition-all duration-300 hover:border-indigo-500/40 shadow-sm">
            <h3 class="text-sm font-black text-slate-855 dark:text-slate-200 tracking-tight border-b border-slate-200 dark:border-slate-800 pb-2 mb-4">
              🎯 {t('buildsnap.stats.category')}
            </h3>
            <div class="space-y-3.5 my-auto">
              <!-- Focus -->
              <div class="space-y-1">
                <div class="flex justify-between items-center">
                  <div class="flex items-center gap-2">
                    <span class="w-2.5 h-2.5 rounded-full bg-violet-500 shrink-0"></span>
                    <span class="text-xs sm:text-sm font-extrabold text-slate-800 dark:text-slate-200 leading-tight">{t('buildsnap.matcher.q1.a')}</span>
                  </div>
                  <span class="text-xs sm:text-sm font-black text-slate-900 dark:text-slate-100 shrink-0">{focusPercent}%</span>
                </div>
                <div class="w-full h-3 bg-slate-200 dark:bg-slate-855 rounded-full overflow-hidden border border-slate-300/30 dark:border-slate-800/30">
                  <div id="barFocus" class="h-full bg-violet-500 rounded-full transition-all duration-1000 ease-out shadow-sm shadow-violet-500/25" style={`width: ${focusPercent}%`}></div>
                </div>
              </div>
              <!-- Creative -->
              <div class="space-y-1">
                <div class="flex justify-between items-center">
                  <div class="flex items-center gap-2">
                    <span class="w-2.5 h-2.5 rounded-full bg-amber-500 shrink-0"></span>
                    <span class="text-xs sm:text-sm font-extrabold text-slate-800 dark:text-slate-200 leading-tight">{t('buildsnap.matcher.q1.b')}</span>
                  </div>
                  <span class="text-xs sm:text-sm font-black text-slate-900 dark:text-slate-100 shrink-0">{creativePercent}%</span>
                </div>
                <div class="w-full h-3 bg-slate-200 dark:bg-slate-855 rounded-full overflow-hidden border border-slate-300/30 dark:border-slate-800/30">
                  <div id="barCreative" class="h-full bg-amber-500 rounded-full transition-all duration-1000 ease-out shadow-sm shadow-amber-500/25" style={`width: ${creativePercent}%`}></div>
                </div>
              </div>
              <!-- Simplicity -->
              <div class="space-y-1">
                <div class="flex justify-between items-center">
                  <div class="flex items-center gap-2">
                    <span class="w-2.5 h-2.5 rounded-full bg-pink-500 shrink-0"></span>
                    <span class="text-xs sm:text-sm font-extrabold text-slate-800 dark:text-slate-200 leading-tight">{t('buildsnap.matcher.q1.c')}</span>
                  </div>
                  <span class="text-xs sm:text-sm font-black text-slate-900 dark:text-slate-100 shrink-0">{simplicityPercent}%</span>
                </div>
                <div class="w-full h-3 bg-slate-200 dark:bg-slate-855 rounded-full overflow-hidden border border-slate-300/30 dark:border-slate-800/30">
                  <div id="barSimplicity" class="h-full bg-pink-500 rounded-full transition-all duration-1000 ease-out shadow-sm shadow-pink-500/25" style={`width: ${simplicityPercent}%`}></div>
                </div>
              </div>
              <!-- Mind -->
              <div class="space-y-1">
                <div class="flex justify-between items-center">
                  <div class="flex items-center gap-2">
                    <span class="w-2.5 h-2.5 rounded-full bg-teal-500 shrink-0"></span>
                    <span class="text-xs sm:text-sm font-extrabold text-slate-800 dark:text-slate-200 leading-tight">{t('buildsnap.matcher.q1.d')}</span>
                  </div>
                  <span class="text-xs sm:text-sm font-black text-slate-900 dark:text-slate-100 shrink-0">{mindPercent}%</span>
                </div>
                <div class="w-full h-3 bg-slate-200 dark:bg-slate-855 rounded-full overflow-hidden border border-slate-300/30 dark:border-slate-800/30">
                  <div id="barMind" class="h-full bg-teal-500 rounded-full transition-all duration-1000 ease-out shadow-sm shadow-teal-500/25" style={`width: ${mindPercent}%`}></div>
                </div>
              </div>
            </div>
          </div>

          <!-- Chart C: Difficulty Barrier -->
          <div class="bg-white/70 dark:bg-slate-900/40 backdrop-blur-md border border-slate-200 dark:border-slate-800/80 p-6 rounded-2xl flex flex-col justify-between select-none transition-all duration-300 hover:border-indigo-500/40 shadow-sm">
            <h3 class="text-sm font-black text-slate-855 dark:text-slate-200 tracking-tight border-b border-slate-200 dark:border-slate-800 pb-2 mb-4">
              🛡️ {t('buildsnap.stats.difficulty')}
            </h3>
            <div class="space-y-6 my-auto py-3">
              <div class="relative w-full h-12 bg-slate-200 dark:bg-slate-855 rounded-2xl overflow-hidden border border-slate-300/60 dark:border-slate-855 flex">
                <div class="h-full bg-gradient-to-r from-emerald-500 to-teal-400 relative overflow-hidden transition-all duration-1000 ease-out" style={`width: ${easyPercent}%`}>
                  <svg class="absolute top-0 right-[-15px] h-full w-[40px] fill-teal-400/85 opacity-90 overflow-visible animate-wave" viewBox="0 0 100 100" preserveAspectRatio="none">
                    <path d="M0,0 Q25,20 0,40 T0,80 T0,100 L100,100 L100,0 Z" />
                  </svg>
                </div>
                <div class="h-full bg-gradient-to-r from-violet-600 to-indigo-500 flex-1 relative overflow-hidden transition-all duration-1000 ease-out">
                  <svg class="absolute top-0 left-[-15px] h-full w-[40px] fill-violet-600/85 opacity-90 overflow-visible animate-wave" viewBox="0 0 100 100" preserveAspectRatio="none" style="transform: rotate(180deg);">
                    <path d="M0,0 Q25,20 0,40 T0,80 T0,100 L100,100 L100,0 Z" />
                  </svg>
                </div>
              </div>
              <div class="flex justify-between items-center text-[10px] sm:text-xs">
                <div class="flex items-center gap-1.5">
                  <span class="w-2.5 h-2.5 rounded-full bg-emerald-500"></span>
                  <span class="font-extrabold text-slate-600 dark:text-slate-400">{t('buildsnap.stats.difficulty.easy')}:</span>
                  <span class="font-black text-slate-855 dark:text-slate-200">{easyPercent}%</span>
                </div>
                <div class="flex items-center gap-1.5">
                  <span class="w-2.5 h-2.5 rounded-full bg-violet-600"></span>
                  <span class="font-extrabold text-slate-600 dark:text-slate-400">{t('buildsnap.stats.difficulty.hard')}:</span>
                  <span class="font-black text-slate-855 dark:text-slate-200">{100 - easyPercent}%</span>
                </div>
              </div>
            </div>
          </div>

          <!-- Chart D: Era distribution (Timeline Wave) -->
          <div class="bg-white/70 dark:bg-slate-900/40 backdrop-blur-md border border-slate-200 dark:border-slate-800/80 p-6 rounded-2xl flex flex-col justify-between select-none transition-all duration-300 hover:border-indigo-500/40 shadow-sm">
            <h3 class="text-sm font-black text-slate-855 dark:text-slate-200 tracking-tight border-b border-slate-200 dark:border-slate-800 pb-2 mb-3">
              🏛️ {t('buildsnap.stats.era')}
            </h3>
            <div class="relative w-full h-24 md:h-auto md:aspect-[400/110] bg-slate-50 dark:bg-slate-955/40 rounded-xl overflow-hidden border border-slate-200/60 dark:border-slate-855">
              <svg viewBox="0 0 400 110" class="w-full h-full">
                <defs>
                  <linearGradient id="areaGrad" x1="0" y1="0" x2="0" y2="1">
                    <addColorStop offset="0%" stop-color="#6366F1" stop-opacity="0.4" id="areaGradStart" />
                    <addColorStop offset="100%" stop-color="#6366F1" stop-opacity="0.0" />
                  </linearGradient>
                </defs>
                <path d="M 0,110 L 0,85 C 50,85 50,100 100,100 C 150,100 150,92 200,92 C 250,92 250,20 300,20 C 350,20 350,75 400,75 L 400,110 Z" fill="url(#areaGrad)" id="waveAreaPath" class="transition-all duration-350" />
                <path d="M 0,85 C 50,85 50,100 100,100 C 150,100 150,92 200,92 C 250,92 250,20 300,20 C 350,20 350,75 400,75" fill="none" stroke="#8b5cf6" stroke-width="3" id="waveCurveLine" class="transition-all duration-350" />
                <circle cx="10" cy="85" r="5.5" class="fill-indigo-500 stroke-slate-950 stroke-[2] cursor-pointer" id="dotAncient" />
                <circle cx="105" cy="100" r="5.5" class="fill-indigo-500 stroke-slate-950 stroke-[2] cursor-pointer" id="dotRenaissance" />
                <circle cx="200" cy="92" r="5.5" class="fill-indigo-500 stroke-slate-950 stroke-[2] cursor-pointer" id="dotEarlyModern" />
                <circle cx="295" cy="20" r="5.5" class="fill-indigo-500 stroke-slate-950 stroke-[2] cursor-pointer" id="dotModern" />
                <circle cx="390" cy="75" r="5.5" class="fill-indigo-500 stroke-slate-950 stroke-[2] cursor-pointer" id="dotContemporary" />
              </svg>
            </div>
            <div class="flex gap-1.5 mt-3 border-t border-slate-200 dark:border-slate-800/80 pt-2.5 overflow-x-auto no-scrollbar whitespace-nowrap pb-1 sm:grid sm:grid-cols-5 sm:gap-2 sm:overflow-visible sm:whitespace-normal sm:pb-0">
              <div id="lblAncient" class="flex flex-col items-center justify-center p-2 sm:px-1 rounded-xl border border-slate-250 dark:border-slate-800 bg-slate-50 dark:bg-slate-900/60 cursor-pointer hover:translate-y-[-2px] transition-all hover:border-indigo-500 text-slate-500 dark:text-slate-400 shrink-0 min-w-[80px]" title={t('buildsnap.era.ancient')}>
                <span class="text-[9px] xs:text-[10px] sm:text-[10px] md:text-xs font-black text-center leading-tight break-keep">{t('buildsnap.era.ancient')}</span>
                <span class="text-[8px] sm:text-[9px] text-slate-400 dark:text-slate-500 font-bold mt-1">{eraCounts.ancient}</span>
              </div>
              <div id="lblRenaissance" class="flex flex-col items-center justify-center p-2 sm:px-1 rounded-xl border border-slate-250 dark:border-slate-800 bg-slate-50 dark:bg-slate-900/60 cursor-pointer hover:translate-y-[-2px] transition-all hover:border-indigo-500 text-slate-500 dark:text-slate-400 shrink-0 min-w-[80px]" title={t('buildsnap.era.renaissance')}>
                <span class="text-[9px] xs:text-[10px] sm:text-[10px] md:text-xs font-black text-center leading-tight break-keep">{t('buildsnap.era.renaissance')}</span>
                <span class="text-[8px] sm:text-[9px] text-slate-400 dark:text-slate-500 font-bold mt-1">{eraCounts.renaissance}</span>
              </div>
              <div id="lblEarlyModern" class="flex flex-col items-center justify-center p-2 sm:px-1 rounded-xl border border-slate-250 dark:border-slate-800 bg-slate-50 dark:bg-slate-900/60 cursor-pointer hover:translate-y-[-2px] transition-all hover:border-indigo-500 text-slate-500 dark:text-slate-400 shrink-0 min-w-[80px]" title={t('buildsnap.era.earlyModern')}>
                <span class="text-[9px] xs:text-[10px] sm:text-[10px] md:text-xs font-black text-center leading-tight break-keep">{t('buildsnap.era.earlyModern')}</span>
                <span class="text-[8px] sm:text-[9px] text-slate-400 dark:text-slate-500 font-bold mt-1">{eraCounts.earlyModern}</span>
              </div>
              <div id="lblModern" class="flex flex-col items-center justify-center p-2 sm:px-1 rounded-xl border border-slate-250 dark:border-slate-800 bg-slate-50 dark:bg-slate-900/60 cursor-pointer hover:translate-y-[-2px] transition-all hover:border-indigo-500 text-slate-500 dark:text-slate-400 shrink-0 min-w-[80px]" title={t('buildsnap.era.modern')}>
                <span class="text-[9px] xs:text-[10px] sm:text-[10px] md:text-xs font-black text-center leading-tight break-keep">{t('buildsnap.era.modern')}</span>
                <span class="text-[8px] sm:text-[9px] text-slate-400 dark:text-slate-500 font-bold mt-1">{eraCounts.modern}</span>
              </div>
              <div id="lblContemporary" class="flex flex-col items-center justify-center p-2 sm:px-1 rounded-xl border border-slate-250 dark:border-slate-800 bg-slate-50 dark:bg-slate-900/60 cursor-pointer hover:translate-y-[-2px] transition-all hover:border-indigo-500 text-slate-500 dark:text-slate-400 shrink-0 min-w-[80px]" title={t('buildsnap.era.contemporary')}>
                <span class="text-[9px] xs:text-[10px] sm:text-[10px] md:text-xs font-black text-center leading-tight break-keep">{t('buildsnap.era.contemporary')}</span>
                <span class="text-[8px] sm:text-[9px] text-slate-400 dark:text-slate-500 font-bold mt-1">{eraCounts.contemporary}</span>
              </div>
            </div>
          </div>
        </div>

      </div>
    </section>

    <!-- 2. Matcher Section (Interactive Wizard Step Slider) -->
    <section id="matcherSection" class="app-card-neon p-4 sm:p-6 md:p-8 mb-8 md:mb-12 relative z-10 border border-slate-200 dark:border-slate-800/80 rounded-3xl overflow-hidden bg-white/40 dark:bg-slate-955/20 backdrop-blur-md">
      <div class="mb-6 text-center md:text-left select-none">
        <h2 class="text-xl sm:text-2xl font-bold text-slate-800 dark:text-slate-100 flex items-center justify-center md:justify-start gap-2">
          <span>{t('buildsnap.matcher.title')}</span>
        </h2>
        <p class="text-xs sm:text-sm text-slate-500 dark:text-slate-400 mt-1">
          {t('buildsnap.matcher.desc')}
        </p>
      </div>

      <!-- 2-Column Responsive Layout Grid -->
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 items-start">
        
        <!-- Left Column (lg:col-span-5): Step Wizard Slider Form -->
        <div class="lg:col-span-5 flex flex-col justify-between min-h-[380px] bg-slate-50/20 dark:bg-slate-955/20 p-4 sm:p-5 rounded-2xl border border-slate-250/30 dark:border-slate-850/30 min-w-0 max-w-full overflow-hidden">
          
          <!-- Step Indicator Track -->
          <div class="flex items-center justify-center gap-2 mb-4 select-none">
            <div id="indicator1" class="w-8 h-2 rounded-full bg-indigo-600 dark:bg-indigo-500 transition-all duration-300"></div>
            <div id="indicator2" class="w-2.5 h-2.5 rounded-full bg-slate-200 dark:bg-slate-800 transition-all duration-300"></div>
          </div>

          <!-- Step Wizard Slider Frame -->
          <div class="relative w-full overflow-hidden flex-1 mb-6">
            <div id="wizardSlider" class="flex transition-transform duration-500 ease-out w-[200%]">
              
              <!-- STEP 1: Age & Gender Selection -->
              <div class="w-1/2 px-1 flex-shrink-0 flex flex-col gap-4">
                <div class="flex-1 bg-slate-50/50 dark:bg-slate-900/30 p-4 rounded-xl border border-slate-200/50 dark:border-slate-800/40">
                  <h3 class="text-xs sm:text-sm font-bold text-slate-855 dark:text-slate-200 mb-3 select-none">
                    👤 {t('buildsnap.matcher.age')}
                  </h3>
                  <div class="grid grid-cols-1 gap-2" id="groupAge">
                    <button type="button" data-age="kids" class="age-btn flex items-center gap-3 p-2.5 rounded-lg border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-950 hover:border-indigo-500 dark:hover:border-indigo-500 text-xs sm:text-sm font-extrabold text-slate-700 dark:text-slate-300 text-left transition-all duration-300 active:scale-[0.98] cursor-pointer">
                      {t('buildsnap.matcher.age.kids')}
                    </button>
                    <button type="button" data-age="adult" class="age-btn active-btn flex items-center gap-3 p-2.5 rounded-lg border border-indigo-500 dark:border-indigo-500 bg-white dark:bg-slate-950 text-xs sm:text-sm font-extrabold text-slate-700 dark:text-slate-300 text-left transition-all duration-300 active:scale-[0.98] cursor-pointer">
                      {t('buildsnap.matcher.age.adult')}
                    </button>
                    <button type="button" data-age="silver" class="age-btn flex items-center gap-3 p-2.5 rounded-lg border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-955 hover:border-indigo-500 dark:hover:border-indigo-500 text-xs sm:text-sm font-extrabold text-slate-700 dark:text-slate-300 text-left transition-all duration-300 active:scale-[0.98] cursor-pointer">
                      {t('buildsnap.matcher.age.silver')}
                    </button>
                  </div>
                </div>

                <div class="flex-1 bg-slate-50/50 dark:bg-slate-900/30 p-4 rounded-xl border border-slate-200/50 dark:border-slate-800/40">
                  <h3 class="text-xs sm:text-sm font-bold text-slate-855 dark:text-slate-200 mb-3 select-none">
                    🧬 {t('buildsnap.matcher.gender')}
                  </h3>
                  <div class="grid grid-cols-1 gap-2" id="groupGender">
                    <button type="button" data-gender="all" class="gender-btn active-btn flex items-center gap-3 p-2.5 rounded-lg border border-indigo-500 dark:border-indigo-500 bg-white dark:bg-slate-955 text-xs sm:text-sm font-extrabold text-slate-700 dark:text-slate-300 text-left transition-all duration-300 active:scale-[0.98] cursor-pointer">
                      {t('buildsnap.matcher.gender.all')}
                    </button>
                    <button type="button" data-gender="male" class="gender-btn flex items-center gap-3 p-2.5 rounded-lg border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-955 hover:border-indigo-500 dark:hover:border-indigo-500 text-xs sm:text-sm font-extrabold text-slate-700 dark:text-slate-300 text-left transition-all duration-300 active:scale-[0.98] cursor-pointer">
                      {t('buildsnap.matcher.gender.male')}
                    </button>
                    <button type="button" data-gender="female" class="gender-btn flex items-center gap-3 p-2.5 rounded-lg border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-955 hover:border-indigo-500 dark:hover:border-indigo-500 text-xs sm:text-sm font-extrabold text-slate-700 dark:text-slate-300 text-left transition-all duration-300 active:scale-[0.98] cursor-pointer">
                      {t('buildsnap.matcher.gender.female')}
                    </button>
                  </div>
                </div>
              </div>

              <!-- STEP 2: Q1, Q2 & Q3 Selection -->
              <div class="w-1/2 px-1 flex-shrink-0 flex flex-col gap-4">
                <div class="bg-slate-50/50 dark:bg-slate-900/30 p-3.5 rounded-xl border border-slate-200/50 dark:border-slate-800/40">
                  <h3 class="text-xs font-bold text-slate-855 dark:text-slate-200 mb-2.5 select-none">
                    🧠 {t('buildsnap.matcher.q1')}
                  </h3>
                  <div class="grid grid-cols-1 sm:grid-cols-2 gap-2" id="groupQ1">
                    {[
                      { val: 'focus', text: t('buildsnap.matcher.q1.a') },
                      { val: 'creative', text: t('buildsnap.matcher.q1.b') },
                      { val: 'simplicity', text: t('buildsnap.matcher.q1.c') },
                      { val: 'mind', text: t('buildsnap.matcher.q1.d') }
                    ].map((ans, idx) => (
                      <button type="button" data-q1={ans.val} class={`q1-btn ${idx === 0 ? 'active-btn border-indigo-500' : 'border-slate-200 dark:border-slate-800'} flex items-center gap-2 p-2.5 rounded-lg border bg-white dark:bg-slate-950 hover:border-indigo-500 text-xs font-extrabold text-slate-700 dark:text-slate-300 text-left transition-all duration-300 active:scale-[0.98] cursor-pointer`}>
                        {ans.text}
                      </button>
                    ))}
                  </div>
                </div>

                <div class="bg-slate-50/50 dark:bg-slate-900/30 p-3.5 rounded-xl border border-slate-200/50 dark:border-slate-800/40">
                  <h3 class="text-xs font-bold text-slate-855 dark:text-slate-200 mb-2.5 select-none">
                    🧭 {t('buildsnap.matcher.q2')}
                  </h3>
                  <div class="grid grid-cols-1 sm:grid-cols-2 gap-2" id="groupQ2">
                    {[
                      { val: 'physical', text: t('buildsnap.matcher.q2.a') },
                      { val: 'mental', text: t('buildsnap.matcher.q2.b') }
                    ].map((ans, idx) => (
                      <button type="button" data-q2={ans.val} class={`q2-btn ${idx === 1 ? 'active-btn border-indigo-500' : 'border-slate-200 dark:border-slate-800'} flex items-center gap-2 p-2.5 rounded-lg border bg-white dark:bg-slate-950 hover:border-indigo-500 text-xs font-extrabold text-slate-700 dark:text-slate-300 text-left transition-all duration-300 active:scale-[0.98] cursor-pointer`}>
                        {ans.text}
                      </button>
                    ))}
                  </div>
                </div>

                <div class="bg-slate-50/50 dark:bg-slate-900/30 p-3.5 rounded-xl border border-slate-200/50 dark:border-slate-800/40">
                  <h3 class="text-xs font-bold text-slate-855 dark:text-slate-200 mb-2.5 select-none">
                    🛠️ {t('buildsnap.matcher.q3')}
                  </h3>
                  <div class="grid grid-cols-1 sm:grid-cols-2 gap-2" id="groupQ3">
                    {[
                      { val: 'easy', text: t('buildsnap.matcher.q3.a') },
                      { val: 'hard', text: t('buildsnap.matcher.q3.b') }
                    ].map((ans, idx) => (
                      <button type="button" data-q3={ans.val} class={`q3-btn ${idx === 0 ? 'active-btn border-indigo-500' : 'border-slate-200 dark:border-slate-800'} flex items-center gap-2 p-2.5 rounded-lg border bg-white dark:bg-slate-950 hover:border-indigo-500 text-xs font-extrabold text-slate-700 dark:text-slate-300 text-left transition-all duration-300 active:scale-[0.98] cursor-pointer`}>
                        {ans.text}
                      </button>
                    ))}
                  </div>
                </div>
              </div>

            </div>
          </div>

          <!-- Navigation buttons for wizard -->
          <div class="flex items-center justify-between pt-4 border-t border-slate-250/40 dark:border-slate-850">
            <button type="button" id="btnPrevStep" class="px-4 py-2 border border-slate-200 dark:border-slate-800 text-slate-500 hover:border-indigo-500 hover:text-indigo-500 text-xs sm:text-sm font-black rounded-lg transition-all cursor-pointer invisible">
              {t('buildsnap.matcher.btn.prev')}
            </button>
            <button type="button" id="btnNextStep" class="px-6 py-2.5 bg-indigo-600 hover:bg-indigo-700 text-white text-xs sm:text-sm font-black rounded-lg shadow-md transition-all cursor-pointer active:scale-95">
              {t('buildsnap.matcher.btn.next')}
            </button>
            <button type="button" id="btnDiagnose" class="px-6 py-2.5 bg-gradient-to-r from-violet-600 to-indigo-600 dark:from-violet-500 dark:to-indigo-500 hover:from-violet-700 hover:to-indigo-700 text-white text-xs sm:text-sm font-black rounded-lg shadow-lg shadow-indigo-500/20 active:scale-95 transition-all cursor-pointer hidden">
              {t('buildsnap.matcher.btn.match')}
            </button>
          </div>

        </div>

        <!-- Right Column (lg:col-span-7): Real-time Result / Pending Dashboard -->
        <div class="lg:col-span-7 w-full">
          
          <!-- Matcher Result Card View (Neon Glass Frame) -->
          <div id="resultContainer" class="hidden p-5 sm:p-6 md:p-8 rounded-2xl bg-white/80 dark:bg-slate-950/80 border border-indigo-500/30 shadow-2xl relative overflow-hidden transition-all duration-500">
            <div class="absolute -right-20 -bottom-20 w-64 h-64 bg-indigo-500/10 dark:bg-indigo-500/15 rounded-full blur-[60px] pointer-events-none z-0"></div>

            <div class="relative z-10 flex flex-col items-center text-center">
              <span class="text-[10px] font-extrabold uppercase tracking-widest text-indigo-550 mb-3 block">{t('buildsnap.result.title')}</span>
              
              <!-- Quick Filter Tuning Chips (Real-time option tuning) -->
              <div id="quickFilterChips" class="flex flex-wrap gap-1.5 justify-center mb-5 max-w-2xl select-none">
                <!-- Dynamically injected chips -->
              </div>

              <div id="matcherStatsBadge" class="px-3.5 py-1 rounded-full text-[10px] sm:text-xs font-black bg-indigo-500/10 dark:bg-indigo-500/20 text-indigo-600 dark:text-indigo-400 border border-indigo-500/20 mb-4 tracking-wide select-none"></div>

              <!-- Hero Illustration Image -->
              <div class="w-full rounded-xl overflow-hidden border border-slate-200 dark:border-slate-850 shadow-lg bg-slate-900 mb-5 relative aspect-video max-h-[300px] w-full max-w-[500px]">
                <img id="resultHeroImage" src="" class="w-full h-full object-cover opacity-90 transition-opacity duration-350" alt="Mentor Habit Illustration" />
              </div>

              <!-- Title and description (Min-height prevent Layout Shift during Shuffle) -->
              <div class="max-w-2xl px-1 mb-5 min-h-[145px] flex flex-col justify-center">
                <h3 id="resultMentorName" class="text-lg sm:text-xl font-black text-slate-900 dark:text-white mb-1.5"></h3>
                <div id="resultHabitName" class="text-[10px] sm:text-xs font-bold text-indigo-650 dark:text-indigo-400 mb-3 uppercase tracking-wider"></div>
                <p id="resultHabitQuote" class="text-xs sm:text-sm md:text-base text-slate-750 dark:text-slate-250 italic leading-relaxed max-w-lg mx-auto"></p>
              </div>

              <!-- Cycle guide instructions -->
              <div id="matcherCycleGuide" class="mt-1 px-3.5 py-1.5 rounded-lg text-[10px] font-bold bg-slate-50 dark:bg-slate-900/50 border border-slate-200/50 dark:border-slate-800/50 text-slate-500 dark:text-slate-400 flex items-center justify-center gap-1.5 select-none"></div>

              <!-- Action buttons for results (Shuffle, Read Story, Reset history, Start Over) -->
              <div class="w-full flex flex-col sm:flex-row flex-wrap items-center justify-center gap-2 pt-5 mt-5 border-t border-slate-100 dark:border-slate-900">
                <button type="button" id="btnShuffle" class="w-full sm:w-auto px-5 py-2.5 bg-emerald-600 hover:bg-emerald-700 text-white rounded-lg text-xs sm:text-sm font-black shadow-md transition-all active:scale-95 select-none cursor-pointer">
                  {t('buildsnap.matcher.btn.shuffle')}
                </button>
                <a href="#" id="btnReadStory" target="_blank" class="w-full sm:w-auto px-5 py-2.5 bg-indigo-600 hover:bg-indigo-700 text-white rounded-lg text-xs sm:text-sm font-black shadow-md transition-all active:scale-95 text-center select-none cursor-pointer">
                  📖 {t('buildsnap.result.button')}
                </a>
                <button type="button" id="btnResetHistory" class="w-full sm:w-auto px-5 py-2.5 border border-slate-200 dark:border-slate-800 text-slate-400 hover:text-rose-500 hover:border-rose-500 rounded-lg text-xs sm:text-sm font-black transition-all active:scale-95 select-none cursor-pointer">
                  {t('buildsnap.matcher.btn.reset')}
                </button>
                <button type="button" id="btnStartOver" class="w-full sm:w-auto px-5 py-2.5 border border-indigo-500/20 text-indigo-650 hover:bg-indigo-600 hover:text-white dark:text-indigo-400 rounded-lg text-xs sm:text-sm font-black transition-all active:scale-95 select-none cursor-pointer flex items-center justify-center gap-1.5">
                  {t('buildsnap.matcher.btn.startover')}
                </button>
              </div>
            </div>
          </div>

          <!-- Matcher Default Placeholder Guide (Before Diagnosis) -->
          <div id="matcherWaitingGuide" class="text-center py-12 px-6 rounded-2xl border border-dashed border-slate-250 dark:border-slate-800/80 bg-slate-50/20 dark:bg-slate-900/10 flex flex-col items-center justify-center min-h-[380px]">
            <span class="text-4xl mb-4 animate-pulse">🔮</span>
            <h4 class="text-sm sm:text-base font-bold text-slate-700 dark:text-slate-300 mb-1.5 select-none">
              {t('buildsnap.matcher.waiting.title')}
            </h4>
            <p class="text-[11px] sm:text-xs text-slate-500 dark:text-slate-400 max-w-sm leading-relaxed">
              {t('buildsnap.matcher.waiting.desc')}
            </p>
          </div>

          <!-- Matcher Empty State -->
          <div id="matcherEmptyState" class="hidden text-center py-10 rounded-2xl bg-slate-100/50 dark:bg-slate-900/20 border border-slate-250/50 dark:border-slate-850 min-h-[380px] flex flex-col items-center justify-center">
            <span class="text-3xl block mb-2">🤔</span>
            <p class="text-xs sm:text-sm font-bold text-slate-500 dark:text-slate-400">{t('buildsnap.matcher.result.none')}</p>
          </div>

        </div>

      </div>
    </section>
    <!-- 4. Smart Nudge Banner Section -->
    <section class="mt-12 md:mt-16 max-w-4xl mx-auto rounded-[2rem] border border-indigo-500/15 bg-indigo-500/5 dark:border-indigo-500/25 dark:bg-indigo-900/10 p-6 md:p-8 flex flex-col md:flex-row items-center gap-6 relative z-10 overflow-hidden">
      <!-- Ambient light effect -->
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-64 h-64 bg-indigo-500/10 rounded-full blur-3xl pointer-events-none"></div>

      <img 
        src={`${import.meta.env.BASE_URL}images/buildself/icon.png`} 
        alt="BuildSelf Mobile App" 
        class="w-16 h-16 sm:w-20 sm:h-20 object-contain rounded-2xl shadow-md border border-indigo-500/10 relative z-10 shrink-0" 
      />
      <div class="flex-1 text-center md:text-left relative z-10 space-y-2 select-none">
        <h3 class="text-lg sm:text-xl font-bold text-slate-800 dark:text-slate-100">{t('buildsnap.nudge.title')}</h3>
        <p class="text-xs sm:text-sm text-slate-650 dark:text-slate-400 font-medium leading-relaxed max-w-xl">
          {t('buildsnap.nudge.desc')}
        </p>
      </div>
      <div class="flex flex-col sm:flex-row gap-3 items-stretch sm:items-center w-full md:w-auto shrink-0 relative z-10">
        <a 
          href={`https://play.google.com/store/apps/details?id=com.selalink.buildself&hl=${currentLang}`} 
          target="_blank" 
          rel="noopener noreferrer" 
          class="px-6 py-3 bg-indigo-600 hover:bg-indigo-700 text-white rounded-xl text-xs sm:text-sm font-extrabold shadow-md hover:scale-[1.01] active:scale-95 transition-all text-center"
        >
          {tUi('app.download_google_play')}
        </a>
        <a 
          href={`/${currentLang}/apps/buildself/`} 
          class="px-6 py-3 bg-slate-900 text-white dark:bg-white dark:text-slate-900 hover:bg-slate-800 dark:hover:bg-slate-100 rounded-xl text-xs sm:text-sm font-extrabold shadow-md border border-slate-750 dark:border-slate-300 hover:scale-[1.01] active:scale-95 transition-all text-center"
        >
          {tUi('app.learn_more_buildself')}
        </a>
      </div>
    </section>
  </main>

  <!-- Client-side logic for diagnostics & canvas generator -->
  <script is:inline define:vars={{ habits, currentLang, runtimeTranslations, eraMentors, timeOfDayCounts, totalHabits, focusPercent, creativePercent, simplicityPercent, mindPercent, habitsTranslations }}>
    window.__BUILDSNAP_DATA__ = { habits, currentLang, runtimeTranslations, eraMentors, timeOfDayCounts, totalHabits, focusPercent, creativePercent, simplicityPercent, mindPercent, habitsTranslations };
  </script>

  <script>
    import { initBuildSnapUI } from './_buildsnapClient';

    document.addEventListener('DOMContentLoaded', () => {
      const data = (window as any).__BUILDSNAP_DATA__;
      if (!data) return;

      initBuildSnapUI(data);
    });
  </script>
</Layout>
